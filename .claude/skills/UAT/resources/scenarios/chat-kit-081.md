# UAT：Chat Kit 0.8.1

关联：[tfrobot-client#79](https://github.com/A2C-SMCP/tfrobot-client/issues/79)。当前记录为验收步骤，尚未执行真实 Beta/macOS Tauri 验收。

## 前置条件与数据

使用已授权测试环境及现有 Manager 账号。准备同一 Robot 的两个可访问会话 A/B、含图片和工具事件的历史，以及可用于下载的测试文件；身份隔离使用已有其他 Robot/账户。不修改 Seed、权限或存储配置。不把真实令牌、签名、附件正文写入报告。

## 用例

| 编号 | 操作 | 预期 |
| --- | --- | --- |
| CK081-01 | 打开 A，输入草稿并添加附件；切到 B，再回 A | 命中缓存时先显示 A 及同步提示，随后显示最新服务端内容；草稿不串会话、不自动发送 |
| CK081-02 | A/B 均加载后断网，从 B 返回 A | 已缓存历史可读，明确同步失败；发送、中断、旧 Ask User 不可作为实时操作执行；恢复网络后手动重选可同步 |
| CK081-03 | 在可控测试身份下使 A 无权限或不存在，再同步 | 清除相应缓存视图，不能继续把其作为可操作会话；权限判断以真实 Server 结果为准 |
| CK081-04 | 关闭缓存或连接提示，再产生新的连接状态/错误 | 关闭只隐藏提示，不恢复权限；新的提示按状态变化重新出现 |
| CK081-05 | 点击时间线工具事件，切换 split/modal 详情；隐藏再返回聊天页面 | 事件详情正常，旧事件导航栏已移除；隐藏页面不留下浮层 |
| CK081-06 | 分别在中文/英文下查看图片、Markdown 资源及工具生成文件，打开和下载 | 加载、动作和错误文案正确；私有附件仍走宿主资源代理与原生动作 |
| CK081-07 | 取消原生保存对话框、切会话或 Robot 取消下载 | 取消不显示失败 Alert，句柄与传输正常释放；无迟到错误污染新上下文 |
| CK081-08 | 菜单往返、快速 A/B/A、切 Robot、切组织/账号及退出登录 | 普通导航保留已有实例；新身份/Robot 不继承旧 client 缓存，迟到结果不覆盖当前会话 |
| CK081-09 | 查看真实 PNG/JPEG、下载并比对字节、打开 PDF/Office/ZIP、播放音视频与拖动、多附件和长时间后重读 | 记录每个实际结果；部署 Front 不支持 Range 时明确能力边界；不能以下载成功推断内嵌预览或模型读取成功 |

## 报告要求

记录客户端版本、六包版本、操作环境、各用例结果与脱敏错误类别。自动化本地 HTTP/Socket.IO 和 jsdom 测试不能替代此表的真实桌面结果。#76 已按代码完成口径关闭，其未完成的真实资源验收仍由本表记录，不自动重开旧 Issue。
