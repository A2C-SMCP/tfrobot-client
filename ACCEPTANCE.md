# TFRobot Client 验收测试文档

> **版本**: v0.1.0
> **覆盖范围**: Phase 1 ~ Phase 6 全功能验收
> **执行环境**: macOS（推荐）/ Windows / Linux
> **前置条件**: 已安装 Node.js、pnpm、Rust toolchain

---

## 0. 环境准备

### 0.1 启动应用

```bash
pnpm install
pnpm tauri dev
```

**预期**: 应用窗口（1200×800）正常打开，左侧导航栏可见，默认停留在「仪表盘」页面。系统托盘出现 TFRobot 图标。

### 0.2 准备测试用 MCP Server

推荐使用一个可快速启动的 stdio 类型 MCP Server 进行测试，例如：
- 命令: `npx`，参数: `@anthropic-ai/mcp-server-fetch` （或任何本地可用的 MCP Server）

---

## 1. Phase 1 — MCP Server 管理

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 1.1 | 添加 Stdio 服务器 | 导航到「MCP 服务器」→ 点击「添加服务器」→ 类型选 Stdio → 填写 name/command/args → 提交 | 服务器出现在列表中，状态显示为 Stopped |
| 1.2 | 添加 HTTP/SSE 服务器 | 同上，类型选 HTTP 或 SSE，填写 name/url | 服务器出现在列表中 |
| 1.3 | 启动服务器 | 点击某服务器的「启动」按钮 | 状态变为 Running（绿色），出现成功提示 |
| 1.4 | 停止服务器 | 点击「停止」按钮 | 状态变为 Stopped |
| 1.5 | 全部启动/停止 | 点击「全部启动」/「全部停止」 | 所有服务器状态统一变化 |
| 1.6 | 编辑服务器 | 点击「编辑」→ 修改参数 → 保存 | 配置更新成功，列表刷新 |
| 1.7 | 删除服务器 | 点击「删除」→ 确认弹窗 → 确认 | 服务器从列表消失 |
| 1.8 | 配置持久化 | 添加服务器后重启应用（Ctrl+C → pnpm tauri dev） | 之前添加的服务器仍在列表中 |

---

## 2. Phase 2 — 连接与变量管理

### 2.1 输入变量

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 2.1.1 | 添加变量 | 导航到「输入变量」→ 添加变量 → 填写 ID/标签/类型 → 保存 | 变量出现在列表中 |
| 2.1.2 | 设置变量值 | 点击变量的「设置值」→ 输入值 → 确认 | 「当前值」列显示设定的值 |
| 2.1.3 | 编辑变量 | 点击编辑 → 修改标签 → 保存 | 显示更新后的标签 |
| 2.1.4 | 删除变量 | 点击删除 → 确认 | 变量从列表移除 |
| 2.1.5 | 清空所有值 | 点击「清空所有值」→ 确认 | 所有变量的当前值清空 |

### 2.2 SMCP 连接

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 2.2.1 | 创建连接配置 | 导航到「SMCP 服务器」→ 添加配置 → 填写 URL/Office ID/计算机名称 → 保存 | 配置出现在列表中 |
| 2.2.2 | 编辑配置 | 点击编辑 → 修改字段 → 保存 | 配置更新成功 |
| 2.2.3 | 删除配置 | 点击删除 → 确认 | 配置从列表移除 |
| 2.2.4 | 连接（有服务器时） | 确保 SMCP 服务器可达 → 点击「连接」 | 连接状态区显示「已连接」、URL、连接时间 |
| 2.2.5 | 断开连接 | 点击「断开」 | 状态变为「未连接」 |
| 2.2.6 | 连接（无服务器时） | 使用一个无效 URL 点击「连接」 | 显示错误提示，不会崩溃 |
| 2.2.7 | API Key 存储 | 创建配置时填写 API Key → 保存 → 重启应用 → 编辑配置 | API Key 通过系统钥匙串存储，配置中有引用标记 |

### 2.3 配置导入/导出

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 2.3.1 | 导出配置 | 「MCP 服务器」页面 → 「导出配置」→ 选择保存路径 | 生成 JSON 文件，包含所有服务器配置 |
| 2.3.2 | 导入配置 | 「导入配置」→ 选择 JSON 文件 | 提示导入了 N 个服务器和 N 个变量 |

---

## 3. Phase 3 — 调试面板与桌面资源

### 3.1 工具浏览器

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 3.1.1 | 工具列表 | 启动至少一个 MCP Server → 导航到「调试面板」→ 「工具」标签 | 列出所有可用工具，显示名称、描述、来源服务器 |
| 3.1.2 | 工具搜索 | 在搜索框输入关键词 | 工具列表实时过滤 |
| 3.1.3 | 服务器筛选 | 切换服务器下拉选择框 | 仅显示选中服务器的工具 |
| 3.1.4 | 工具详情 | 点击某工具 | 右侧显示 inputSchema 结构化视图 |

### 3.2 工具调用测试

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 3.2.1 | 表单模式调用 | 选择工具 → 在自动生成的表单中填写参数 → 点击「执行」 | 返回结果区展示工具输出（文本/图片）和耗时 |
| 3.2.2 | JSON 模式调用 | 切换到 JSON 模式 → 手动编辑 JSON → 执行 | 同上，结果正确返回 |
| 3.2.3 | 表单/JSON 切换 | 在表单模式填写参数 → 切换到 JSON 模式 | JSON 中包含表单中填写的值 |
| 3.2.4 | 超时设置 | 设置超时时间 → 调用一个可能耗时的工具 | 超时后返回超时错误信息 |
| 3.2.5 | 错误处理 | 传入无效参数执行工具 | 显示错误信息，不崩溃 |

### 3.3 调用历史

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 3.3.1 | 历史记录 | 执行几次工具调用 → 切到「历史记录」标签 | 按时间倒序显示调用记录（工具名、服务器、状态） |
| 3.3.2 | 重放功能 | 点击历史记录的「重放」按钮 | 跳回工具标签，工具被选中，参数自动填充 |

### 3.4 桌面资源

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 3.4.1 | 资源列表 | 导航到「桌面资源」→ 点击「刷新」 | 显示 window:// URI 列表（如果有提供资源的 MCP Server 运行中） |
| 3.4.2 | 无资源提示 | 没有 MCP Server 运行时查看 | 显示「暂无桌面窗口」提示 |

---

## 4. Phase 4 — 日志与仪表盘

### 4.1 日志查看器

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 4.1.1 | 日志自动产生 | 启动应用 → 操作（添加服务器、启停、连接等） → 导航到「日志」 | 可见操作对应的日志条目（info 级别，分类为 system/mcp/connection/tool） |
| 4.1.2 | 时间快捷筛选 | 点击「1h」「6h」「24h」「7d」按钮 | 日志列表仅显示对应时间范围内的记录 |
| 4.1.3 | 全部时间 | 点击「All」按钮 | 显示所有日志 |
| 4.1.4 | 级别筛选 | 在「按级别筛选」中选择 ERROR | 仅显示 error 级别日志 |
| 4.1.5 | 分类筛选 | 在「按分类筛选」中选择 mcp | 仅显示 mcp 分类日志 |
| 4.1.6 | 关键词搜索 | 输入关键词搜索 | 仅显示消息包含该关键词的日志 |
| 4.1.7 | 组合筛选 | 同时设置时间 + 级别 + 关键词 | 筛选条件叠加生效 |
| 4.1.8 | 导出日志 | 点击「导出」→ 选择路径 | 生成 JSON 文件，内容为当前筛选条件下的日志 |
| 4.1.9 | 清空日志 | 点击「清除」→ 确认弹窗 → 确认 | 日志列表清空 |
| 4.1.10 | 详情展开 | 如有包含 details 的日志行，点击展开 | 行下方展示 details 详细内容 |
| 4.1.11 | 分页 | 日志较多时 | 分页器可用，可切换每页条数（20/50/100） |

### 4.2 仪表盘

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 4.2.1 | 默认页面 | 启动应用 | 默认进入仪表盘页面 |
| 4.2.2 | 连接状态卡片 | 查看仪表盘 | 显示当前连接状态（已连接/未连接），如已连接显示 URL 和配置名 |
| 4.2.3 | MCP 服务器卡片 | 查看仪表盘 | 显示总数、运行中数量、停止数量 |
| 4.2.4 | 工具数量卡片 | 查看仪表盘 | 显示当前可用工具总数 |
| 4.2.5 | 运行时卡片 | 查看仪表盘 | 显示 Node.js/Python/uv/pnpm 的检测结果（绿色=已安装，红色=未安装），已安装的显示路径 |
| 4.2.6 | 最近活动卡片 | 查看仪表盘 | 显示最近 10 条日志（时间、级别、消息） |
| 4.2.7 | 卡片点击跳转 | 点击「连接状态」卡片 | 跳转到 SMCP 服务器页面 |
| 4.2.8 | 卡片点击跳转 | 点击「MCP 服务器」卡片 | 跳转到 MCP 服务器页面 |
| 4.2.9 | 卡片点击跳转 | 点击「工具」卡片 | 跳转到调试面板 |
| 4.2.10 | 卡片点击跳转 | 点击「最近活动」卡片 | 跳转到日志页面 |
| 4.2.11 | 刷新按钮 | 点击仪表盘右上角「刷新」 | 所有卡片数据重新加载 |

---

## 5. Phase 5 — 主题与设置

### 5.1 主题切换

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 5.1.1 | Header 主题按钮 | 点击 Header 右侧的太阳/月亮图标 | 界面在亮色/暗色之间切换，所有 Ant Design 组件样式正确响应 |
| 5.1.2 | 设置页切换 | 导航到「设置」→「外观」→ 选择 Light/Dark/System | 主题立即切换 |
| 5.1.3 | 跟随系统 | 选择 System 模式 → 在系统设置中切换深色/浅色外观 | 应用实时跟随系统主题变化 |
| 5.1.4 | 主题持久化 | 切换到 Dark 模式 → 重启应用 | 重启后仍为 Dark 模式 |
| 5.1.5 | 暗色模式完整性 | 在暗色模式下浏览所有页面 | 无硬编码白色背景残留，表格/卡片/表单/弹窗均正确显示 |

### 5.2 语言切换

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 5.2.1 | Header 语言按钮 | 点击 Header 右侧的「中/EN」按钮 | 界面语言在中文/英文间切换，所有文本即时更新 |
| 5.2.2 | 设置页切换 | 「设置」→「外观」→ 语言下拉选择 | 同上 |

### 5.3 运行时设置

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 5.3.1 | 运行时检测 | 「设置」→「运行时」标签 | 表格显示 Node.js/Python/uv/pnpm 的路径、版本、安装状态 |
| 5.3.2 | 版本显示 | 观察已安装运行时的版本列 | 显示实际版本号（如 v20.x.x、Python 3.x.x） |
| 5.3.3 | 安装引导 | 对于未安装的运行时，点击「安装指引」 | 浏览器打开对应运行时的官方网站 |
| 5.3.4 | 自定义路径 | 展开「自定义路径」→ 填入自定义路径 → 关闭再打开设置 | 路径值保持不变（已持久化） |
| 5.3.5 | 刷新检测 | 点击「刷新」按钮 | 重新执行运行时检测，更新表格 |

### 5.4 数据管理

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 5.4.1 | 日志保留天数 | 「设置」→「数据管理」→ 修改日志保留天数 | 数值修改成功，重启后生效（旧日志在启动时清理） |
| 5.4.2 | 导出所有配置 | 点击「导出所有配置」→ 选择路径 | 生成包含所有服务器配置的 JSON 文件 |
| 5.4.3 | 清空日志 | 点击「清除」日志 → 确认 | 日志页面变为空 |

### 5.5 关于

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 5.5.1 | 版本信息 | 「设置」→「关于」标签 | 显示应用版本（0.1.0）、SDK 版本、许可证（MIT） |
| 5.5.2 | 检查更新 | 点击「检查更新」 | 提示当前已是最新版本（或显示更新弹窗） |
| 5.5.3 | 反馈链接 | 点击「反馈」 | 浏览器打开 GitHub 仓库页面 |

### 5.6 MCP 高级配置

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 5.6.1 | 高级设置折叠 | 添加/编辑 MCP 服务器 → 展开「高级设置」 | 显示 Disabled 开关、Forbidden Tools、Tool Meta JSON、VRL 脚本字段 |
| 5.6.2 | Disabled 开关 | 打开 Disabled → 保存 | 服务器配置中 disabled 为 true |
| 5.6.3 | Forbidden Tools | 输入工具名（回车或逗号分隔）→ 保存 | 配置中包含 forbidden_tools 列表 |
| 5.6.4 | Tool Meta JSON | 输入有效 JSON → 保存 | 配置中 tool_meta 包含输入的内容 |
| 5.6.5 | VRL 脚本 | 输入脚本文本 → 保存 | 不报错，配置正常保存 |

---

## 6. Phase 6 — 系统集成与分发

### 6.1 系统托盘

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 6.1.1 | 托盘图标 | 启动应用 | 系统托盘出现 TFRobot 图标 |
| 6.1.2 | 左键点击 | 点击托盘图标 | 如果窗口隐藏，则显示并聚焦窗口 |
| 6.1.3 | 右键菜单 | 右键托盘图标 | 弹出菜单，包含「Show Window」和「Quit」两项 |
| 6.1.4 | Show Window | 隐藏窗口后 → 右键 → Show Window | 窗口恢复显示并获得焦点 |
| 6.1.5 | Quit | 右键 → Quit | 应用完全退出，托盘图标消失 |

### 6.2 最小化到托盘

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 6.2.1 | 关闭按钮行为 | 点击窗口关闭按钮（红色 × / macOS 红点） | 窗口隐藏但不退出，托盘图标仍在 |
| 6.2.2 | 恢复窗口 | 窗口隐藏后点击托盘图标 | 窗口恢复显示 |
| 6.2.3 | 后台运行确认 | 隐藏窗口后检查系统活动监视器/任务管理器 | TFRobot Client 进程仍在运行 |

### 6.3 单实例保护

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 6.3.1 | 阻止多实例 | 应用运行中 → 再次执行 `pnpm tauri dev`（或双击应用） | 不会启动第二个实例，已有窗口被聚焦显示 |

### 6.4 优雅退出

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 6.4.1 | 退出日志 | 通过托盘 Quit 退出 → 重新启动 → 查看日志 | 日志中包含「Application shutting down」和「Application started」记录 |
| 6.4.2 | 连接清理 | 连接 SMCP → 通过 Quit 退出 | 退出前自动断开连接（服务端可观察到客户端离开） |

### 6.5 CI/CD

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 6.5.1 | Workflow 文件 | 检查 `.github/workflows/release.yml` | 文件存在，包含 macOS (arm64 + x86_64)、Ubuntu、Windows 四个构建矩阵 |
| 6.5.2 | 触发条件 | 查看 workflow 配置 | 仅在推送 `v*` tag 时触发 |

---

## 7. 跨功能验收

| # | 验收项 | 操作步骤 | 预期结果 |
|---|--------|----------|----------|
| 7.1 | 国际化完整性 | 在中文和英文模式下浏览所有页面 | 无未翻译的 key（不出现形如 `settings.xxx` 的原始 key） |
| 7.2 | 暗色模式全页面 | 切换到暗色模式，依次浏览所有 8 个页面 | 无白色背景异常、文字不可读等视觉问题 |
| 7.3 | 导航完整性 | 依次点击侧边栏所有菜单项 | 每个菜单项对应正确的页面内容，无空白页 |
| 7.4 | 数据持久化 | 添加服务器 + 变量 + 连接配置 + 修改设置 → 重启应用 | 所有数据保持不变 |
| 7.5 | 错误恢复 | 在无 MCP Server 运行时访问调试面板、仪表盘 | 显示空状态提示，不崩溃 |
| 7.6 | TypeScript 编译 | 执行 `npx tsc --noEmit` | 无错误 |
| 7.7 | 前端测试 | 执行 `pnpm test` | 75 个测试全部通过 |
| 7.8 | Rust 编译 | 执行 `cd src-tauri && cargo check` | 编译成功无错误 |

---

## 附录：缺陷严重等级

| 等级 | 定义 | 示例 |
|------|------|------|
| **P0 阻断** | 功能完全不可用或应用崩溃 | 点击按钮白屏、Rust panic |
| **P1 严重** | 核心功能异常但有 workaround | 服务器添加后列表不刷新（手动刷新可解） |
| **P2 一般** | 非核心功能缺陷或体验问题 | 暗色模式下某处颜色不协调 |
| **P3 建议** | 优化建议 | 翻译措辞可改进 |
