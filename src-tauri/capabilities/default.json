{
  "$schema": "https://schemas.tauri.app/v2/capability",
  "identifier": "default",
  "description": "Default capabilities for TFRobot Client",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-spawn",
    "shell:allow-kill",
    "updater:default",
    "dialog:default"
  ]
}
